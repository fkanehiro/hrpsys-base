add_subdirectory(ProjectGenerator)
add_subdirectory(SelfCollisionChecker)
if(USE_HRPSYSUTIL)
  option(USE_HRPSYSEXT "Build hrpsys Python bindings" ON)
  if(USE_HRPSYSEXT)
    add_subdirectory(simulator)
  endif()
  add_subdirectory(viewer)
  add_subdirectory(monitor)
endif()
